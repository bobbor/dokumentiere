for item in self.index
	section.col.col-3(data-type="#{item.type}",data-name="#{item.name}")
		h1
			a(href="#{((item.scope || item.visibility)+'.'+(item && item.name))}.html")= item.name
		h2 #{item.type} (#{item.scope || item.visibility})
		p= item.description[0].desc
